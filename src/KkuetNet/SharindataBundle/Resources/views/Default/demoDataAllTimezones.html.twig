{% extends "::base.html.twig" %}

{% block body %}
<div class="row">
    <div class="col-lg-12">
        <h2>Request</h2>
        <pre class="brush: php;">
            $response = \KkuetNet\SharindataClientApi\Vendor\SharindataClientApi::getInstance("your_login", "your_password");
            var_dump($response->getCountries());
        </pre>
        <h2>JSON Response</h2>
        <pre class="brush: js;">
            {{data1}}
        </pre>
        
        <h2>Example of result</h2>
        <ul class="custom-liste col-lg-6">
            {% for k, j in json_data1|slice(0, json_data1|length/2) %}
                <li><img src="{{ j.flag }}"/> <b><i class="code">{{j.name}}</i></b> (&#9742; <i class="code">+{{ j.call_prefix }}</i>)</li>
            {% endfor %}
        </ul>
        <ul class="custom-liste col-lg-6">
            {% for k, j in json_data1|slice(json_data1|length/2, json_data1|length-json_data1|length/2) %}
                <li><img src="{{ j.flag }}"/> <b><i class="code">{{j.name}}</i></b> (&#9742; <i class="code">+{{ j.call_prefix }}</i>)</li>
            {% endfor %}
        </ul>
    </div>
</div>
{% endblock %}