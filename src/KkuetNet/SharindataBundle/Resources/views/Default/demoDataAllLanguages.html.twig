{% extends "::base.html.twig" %}

{% block body %}
<div class="row">
    <div class="col-lg-12">
        <h2>Request</h2>
        <pre class="brush: php;">
            $response = \KkuetNet\SharindataClientApi\Vendor\SharindataClientApi::getInstance("your_api_key", "your_api_secret");
            var_dump($response->getLanguages());
        </pre>
        <h2>JSON Response</h2>
        <pre class="brush: js;">
            {{data1}}
        </pre>
        
        <h2>Example of result</h2>
        <ul class="custom-liste col-lg-4">
            {% for k, j in json_data1|slice(0, json_data1|length/3) %}
                 <li>{{ j.name_en }} ({{ j.iso_639_1 }}) - {{j.natural_name}}</li>
            {% endfor %}
        </ul>
        <ul class="custom-liste col-lg-4">
            {% for k, j in json_data1|slice(json_data1|length/3, json_data1|length-(json_data1|length/3)*2) %}
                 <li>{{ j.name_en }} ({{ j.iso_639_1 }}) - {{j.natural_name}}</li>
            {% endfor %}
        </ul>
        <ul class="custom-liste col-lg-4">
            {% for k, j in json_data1|slice((json_data1|length/3)*2, json_data1|length-json_data1|length/3) %}
                 <li>{{ j.name_en }} ({{ j.iso_639_1 }}) - {{j.natural_name}}</li>
            {% endfor %}
        </ul>
    </div>
</div>
{% endblock %}